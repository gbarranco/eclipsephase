{% extends 'base.html.twig' %}

{% block title %}{% trans %}Player{% endtrans %} : {{ player.playerName }}{% endblock %}

{% block body %}
    <h1>{% trans %}Player{% endtrans %} : {{ player.playerName }}</h1>

    <table class="table table-hover table-striped table-light">
        <tbody>
            <tr>
                <th scope="row">{% trans %}Player name{% endtrans %}</th>
                <td>{{ player.playerName }}</td>
            </tr>
            <tr>
                <th scope="row">{% trans %}Player type{% endtrans %}</th>
                <td>{{ player.playerType }}</td>
            </tr>
        </tbody>
    </table>

    <div class="back">
        <a href="{{ path('profil_edit', {'id': app.user.id}) }}">
            <i class="far fa-edit">
                {% trans %}edit{% endtrans %}
            </i>
        </a>
    </div>

    <div class="back">
        <a href="{{ path('home') }}">
            <i class="fas fa-angle-double-left">
                {% trans %}back{% endtrans %}
            </i>
        </a>
    </div>

    {{ include('player/_delete_form.html.twig') }}
{% endblock %}
